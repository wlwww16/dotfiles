#!/bin/sh

task=$1
value=$2

if [ $task = "add" ]
then
	light -A $value
	notify-send "Brightness" "Increased by $value ($(echo $(light) | awk '{print int($1)}'))"
else
	light -U $value
	notify-send "Brightness" "Decreased by $value ($(echo $(light) | awk '{print int($1)}'))"
fi
