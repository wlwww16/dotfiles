#!/bin/sh

selected=$(dmenu_prompt "Silent\nBalanced\nPerformance" "Select a power mode")
number=0

case $selected in
	Silent) number=2 ;;
	Balanced) number=0 ;;
	Performance) number=1 ;;
esac

echo $number | sudo -A tee /sys/devices/platform/asus-nb-wmi/throttle_thermal_policy
